<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 配置自动扫描的包 -->
    <!--
    若 Spring IOC 容器 和 SpringMVC 的IOC容器扫描的包有重合的部分，就会导致部分bean被创建两次
    解决办法：
        1、让两个容器扫描的包没有重合的部分
        2、使用 context:include-filter 和 context:exclude-filter子节点来规定扫描的包
     -->
    <!--
     SpringMVC IOC 容器中的bean可以引用 Spring IOC容器中的 bean，反之则不然
     -->
    <context:component-scan base-package="com.springmvc" use-default-filters="false">
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
        <context:include-filter type="annotation" expression="org.springframework.web.bind.annotation.ControllerAdvice"/>
    </context:component-scan>

    <!-- 配置视图解析器 -->
    <bean id="internalResourceViewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"></property>
        <property name="suffix" value=".jsp"></property>
    </bean>

    <!-- 添加这个配置，在springmvc配置文件中没有映射的情况下，会去寻找资源 -->
    <mvc:default-servlet-handler></mvc:default-servlet-handler>
    <!-- 添加了上面的配置后，必须添加这个配置，否则@RequestMapping注解将会失效 -->
    <mvc:annotation-driven></mvc:annotation-driven>

</beans>